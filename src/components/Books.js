import React from 'react'
import { BookCard } from './BookCard'

export const Books = () => {
  return (
    <section className='container container--grid'>
        <BookCard />
        <BookCard />
        <BookCard />
        <BookCard />
        <BookCard />
        <BookCard />
        <BookCard />
    </section>
  )
}
